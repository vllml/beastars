# Core ML dependencies (PyTorch installed separately with CUDA support)
transformers>=4.35.0
peft>=0.7.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
datasets>=2.14.0
sentencepiece>=0.1.99
protobuf>=3.20.0
packaging>=21.0
ninja>=1.11.0
einops>=0.7.0
safetensors>=0.4.0
huggingface-hub>=0.19.0
tokenizers>=0.15.0
litgpt>=0.2.0
axolotl>=0.4.0
wandb>=0.16.0
tensorboard>=2.15.0

# Optional: flash-attn (install separately if needed)
# flash-attn>=2.3.0; sys_platform != 'win32'
